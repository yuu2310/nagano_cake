<%= render 'public/nav' %>
<div>
  <div class="col-lg-12">
    <div>
      <strong class="information-link"><h3>ショッピングカート</h3></strong>
      <%= link_to "カートを空にする", cart_items_destroy_all_path, method: :delete, class: "pull-right btn btn-danger" %>
    </div>
        
        
    <table class="table table-hover table-bordered col-xs-10" >
      
      
        <thead>
          <tr>
            <th class="col-xs-2">商品名</th>
            <th class="col-xs-2">単価(税込)</th>
            <th class="col-xs-1">数量</th>
            <th class="col-xs-1">小計</th>
            <th class="col-xs-1"></th>
          </tr>
        </thead>
        
        <tbody>
          <% @cart_items.each do |cart_item| %>
           
              <tr>
                
                <td>
                  <%= image_tag cart_item.item.image, size: "130x100" %><%= cart_item.item.name %>
                </td>
                <%#= image_tag @user.get_profile_image("80x80") %>
                
                <td>¥ <%= cart_item.item.with_tax_price.to_s(:delimited) %></td>
              
                <td>
                  <%= form_with model: cart_item, url: cart_item_path(cart_item.id), method: :patch do |f| %><%= f.select :amount, *[1..10] %>
                  <%= f.submit '変更', class: "pull-right btn btn-success " %>
                  <% end %>
                </td>
               
                
                <td>¥ <%= cart_item.subtotal %></td>
                
                <td><%= link_to '削除する', cart_item_path(cart_item.id), method: :delete %></td>
                
                 <% @total += cart_item.subtotal %>
                 
              </tr>
            
          <% end %>
        </tbody>
    </table>
  </div>
</div>

<div class="col-lg-3 float-right ">
  <table class="table table-bordered">
    <tr>
      <th>合計金額</th>
      <th>¥ <%= @total %></th>
    </tr>
  </table>
</div>

  
 <%= link_to '買い物を続ける', root_path, class: "pull-right btn btn-primary" %>

<%= link_to '情報入力に進む', new_order_path, class: "pull-right btn btn-success" %>